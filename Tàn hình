--[[
    King Tower - Mastermind Identity Swap Script (Real Body = NPC God, Clone = Player Bait)
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

if _G.MastermindIdentityCoreLoaded then return end
_G.MastermindIdentityCoreLoaded = true

-- MAIN UI INTERFACE SETUP
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MastermindIdentityUI"
ScreenGui.ResetOnSpawn = false
pcall(function() ScreenGui.Parent = CoreGui end)
if not ScreenGui.Parent then ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 260, 0, 230)
MainFrame.Position = UDim2.new(0, 50, 0, 150)
MainFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICornerMain = Instance.new("UICorner")
UICornerMain.CornerRadius = UDim.new(0, 12)
UICornerMain.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 3
UIStroke.Parent = MainFrame

task.spawn(function()
    local hue = 0
    while true do
        hue = (hue + 0.01) % 1
        UIStroke.Color = Color3.fromHSV(hue, 1, 1)
        task.wait(0.03)
    end
end)

local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 30)
TopBar.BackgroundTransparency = 1
TopBar.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -35, 1, 0)
TitleLabel.Position = UDim2.new(0, 10, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "Mastermind NPC Swap Hub"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 11
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TopBar

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 25, 0, 25)
MinimizeBtn.Position = UDim2.new(1, -28, 0, 3)
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
MinimizeBtn.Text = "-"
MinimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinimizeBtn.TextSize = 16
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.Parent = TopBar

local UICornerMin = Instance.new("UICorner")
UICornerMin.CornerRadius = UDim.new(0, 6)
UICornerMin.Parent = MinimizeBtn

local Container = Instance.new("Frame")
Container.Size = UDim2.new(1, -20, 0, 130)
Container.Position = UDim2.new(0, 10, 0, 40)
Container.BackgroundTransparency = 1
Container.Parent = MainFrame

local InfoLabel = Instance.new("TextLabel")
InfoLabel.Size = UDim2.new(1, -20, 0, 25)
InfoLabel.Position = UDim2.new(0, 10, 0, 195)
InfoLabel.BackgroundTransparency = 1
InfoLabel.Text = "Status: Ready to Execute Swap Strategy"
InfoLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
InfoLabel.TextSize = 10
InfoLabel.Font = Enum.Font.Gotham
InfoLabel.TextXAlignment = Enum.TextXAlignment.Center
InfoLabel.Parent = MainFrame

-- CONTROL BUTTONS
local ActivateSwapBtn = Instance.new("TextButton")
ActivateSwapBtn.Size = UDim2.new(1, 0, 0, 38)
ActivateSwapBtn.Position = UDim2.new(0, 0, 0, 0)
ActivateSwapBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
ActivateSwapBtn.TextColor3 = Color3.fromRGB(0, 255, 128)
ActivateSwapBtn.TextSize = 11
ActivateSwapBtn.Font = Enum.Font.GothamBold
ActivateSwapBtn.Text = "🎭 Execute Identity Swap (Real=NPC, Clone=Player)"
ActivateSwapBtn.Parent = Container

local UIC_1 = Instance.new("UICorner") UIC_1.CornerRadius = UDim.new(0, 6) UIC_1.Parent = ActivateSwapBtn

local CamRealBtn = Instance.new("TextButton")
CamRealBtn.Size = UDim2.new(1, 0, 0, 38)
CamRealBtn.Position = UDim2.new(0, 0, 0, 45)
CamRealBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
CamRealBtn.TextColor3 = Color3.fromRGB(255, 220, 100)
CamRealBtn.TextSize = 11
CamRealBtn.Font = Enum.Font.GothamBold
CamRealBtn.Text = "👁️ View Camera on Real Body (NPC)"
CamRealBtn.Parent = Container

local UIC_2 = Instance.new("UICorner") UIC_2.CornerRadius = UDim.new(0, 6) UIC_2.Parent = CamRealBtn

local CamCloneBtn = Instance.new("TextButton")
CamCloneBtn.Size = UDim2.new(1, 0, 0, 38)
CamCloneBtn.Position = UDim2.new(0, 0, 0, 90)
CamCloneBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
CamCloneBtn.TextColor3 = Color3.fromRGB(100, 200, 255)
CamCloneBtn.TextSize = 11
CamCloneBtn.Font = Enum.Font.GothamBold
CamCloneBtn.Text = "👁️ View Camera on Clone (Bait Player)"
CamCloneBtn.Parent = Container

local UIC_3 = Instance.new("UICorner") UIC_3.CornerRadius = UDim.new(0, 6) UIC_3.Parent = CamCloneBtn

-- MINIMIZE LOGIC
local isMinimized = false
MinimizeBtn.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    if isMinimized then
        MinimizeBtn.Text = "+"
        Container.Visible = false
        InfoLabel.Visible = false
        TweenService:Create(MainFrame, TweenInfo.new(0.2), {Size = UDim2.new(0, 260, 0, 35)}):Play()
    else
        MinimizeBtn.Text = "-"
        Container.Visible = true
        InfoLabel.Visible = true
        TweenService:Create(MainFrame, TweenInfo.new(0.2), {Size = UDim2.new(0, 260, 0, 230)}):Play()
    end
end)

-- CORE LOGIC VARIABLES
local activeClone = nil
local realMoveConnection = nil

-- EXECUTE IDENTITY SWAP
ActivateSwapBtn.MouseButton1Click:Connect(function()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then 
        InfoLabel.Text = "Status: Character not found!"
        return 
    end
    
    local startCF = character.HumanoidRootPart.CFrame
    
    if activeClone and activeClone.Parent then
        activeClone:Destroy()
        activeClone = nil
    end
    
    -- 1. BIẾN CƠ THỂ GỐC THÀNH NPC (Đổi tên để ngắt liên kết nhận diện player trực tiếp)
    character.Name = "InertNPC_GodBody"
    local realHumanoid = character:FindFirstChildOfClass("Humanoid")
    
    if realHumanoid then
        -- GODMODE CHO REAL BODY (Bất tử hoàn toàn, nhận sát thương nhưng máu lập tức hồi đầy)
        realHumanoid.MaxHealth = math.huge
        realHumanoid.Health = math.huge
        realHumanoid.HealthChanged:Connect(function()
            if realHumanoid and realHumanoid.Health < realHumanoid.MaxHealth then
                realHumanoid.Health = realHumanoid.MaxHealth
            end
        end)
    end
    
    -- 2. TẠO CLONE MANG NGUYÊN VẸN DANH TÍNH PLAYER ĐỂ ĐỨNG Ở KHU HỒI SINH/DƯỚI ĐẤT
    character.Archivable = true
    activeClone = character:Clone()
    character.Archivable = false
    
    -- Khôi phục tên gốc cho Clone để nó giữ trọn vẹn đặc tính Player trước mắt Server
    activeClone.Name = LocalPlayer.Name
    activeClone.Parent = workspace
    activeClone:SetPrimaryPartCFrame(startCF + Vector3.new(0, 2, 0))
    
    local cloneHumanoid = activeClone:FindFirstChildOfClass("Humanoid")
    local cloneHrp = activeClone:FindFirstChild("HumanoidRootPart")
    
    if cloneHumanoid then
        cloneHumanoid.PlatformStand = false
        cloneHumanoid.AutoRotate = true
        -- Hướng góc nhìn camera bám theo Real Body để bạn tự do điều khiển Real Body đi quậy
        Camera.CameraSubject = realHumanoid
    end
    
    -- Thiết lập va chạm cho Clone làm mồi nhử
    for _, part in ipairs(activeClone:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = true
        end
    end
    
    -- 3. ĐIỀU KHIỂN REAL BODY (NPC) HOẠT ĐỘNG TỰ DO KHẮP NƠI
    if realMoveConnection then realMoveConnection:Disconnect() end
    realMoveConnection = RunService.RenderStepped:Connect(function()
        if character and character.Parent and realHumanoid then
            local moveDir = Vector3.zero
            if UserInputService:IsKeyDown(Enum.KeyCode.W) or UserInputService:IsKeyDown(Enum.KeyCode.Up) then
                moveDir = moveDir + Vector3.new(0, 0, -1)
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) or UserInputService:IsKeyDown(Enum.KeyCode.Down) then
                moveDir = moveDir + Vector3.new(0, 0, 1)
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) or UserInputService:IsKeyDown(Enum.KeyCode.Left) then
                moveDir = moveDir + Vector3.new(-1, 0, 0)
            end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) or UserInputService:IsKeyDown(Enum.KeyCode.Right) then
                moveDir = moveDir + Vector3.new(1, 0, 0)
            end
            
            if moveDir.Magnitude > 0 then
                local camLook = Camera.CFrame.LookVector
                camLook = Vector3.new(camLook.X, 0, camLook.Z).Unit
                local camRight = Camera.CFrame.RightVector
                camRight = Vector3.new(camRight.X, 0, camRight.Z).Unit
                
                local worldDir = (camRight * moveDir.X + camLook * -moveDir.Z).Unit
                realHumanoid:Move(worldDir, false)
            end
        end
    end)
    
    InfoLabel.Text = "Status: Swap Success! Real Body is NPC God."
end)

-- CAMERA SWITCH CONTROLS
CamRealBtn.MouseButton1Click:Connect(function()
    local character = LocalPlayer.Character
    if character and character:FindFirstChildOfClass("Humanoid") then
        Camera.CameraSubject = character:FindFirstChildOfClass("Humanoid")
        InfoLabel.Text = "Status: Camera on Real Body (NPC)"
    end
end)

CamCloneBtn.MouseButton1Click:Connect(function()
    if activeClone and activeClone:FindFirstChildOfClass("Humanoid") then
        Camera.CameraSubject = activeClone:FindFirstChildOfClass("Humanoid")
        InfoLabel.Text = "Status: Camera on Clone (Player Bait)"
    end
end)

-- CLEANUP ON RESET
LocalPlayer.CharacterAdded:Connect(function()
    if realMoveConnection then realMoveConnection:Disconnect() realMoveConnection = nil end
    if activeClone then
        activeClone:Destroy()
        activeClone = nil
    end
    InfoLabel.Text = "Status: Reset - Normal Mode"
end)
