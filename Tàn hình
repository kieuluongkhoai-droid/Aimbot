--[[
    King Tower - Giant Floor & Perfect Swap Hub
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

if _G.GiantFloorConnection then
    pcall(function() _G.GiantFloorConnection:Disconnect() end)
    _G.GiantFloorConnection = nil
end

pcall(function()
    if CoreGui:FindFirstChild("GiantFloorUI") then
        CoreGui.GiantFloorUI:Destroy()
    end
end)

pcall(function()
    if workspace:FindFirstChild("HiddenArmorSeat") then
        workspace.HiddenArmorSeat:Destroy()
    end
    if workspace:FindFirstChild("UndergroundGiantFloor") then
        workspace.UndergroundGiantFloor:Destroy()
    end
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GiantFloorUI"
ScreenGui.ResetOnSpawn = false
pcall(function() ScreenGui.Parent = CoreGui end)
if not ScreenGui.Parent then ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 270, 0, 220)
MainFrame.Position = UDim2.new(0, 50, 0, 150)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICornerMain = Instance.new("UICorner")
UICornerMain.CornerRadius = UDim.new(0, 12)
UICornerMain.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 2
UIStroke.Color = Color3.fromRGB(0, 255, 128)
UIStroke.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0, 30)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "🏗️ Giant Floor & Swap Hub"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 11
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Parent = MainFrame

local Container = Instance.new("Frame")
Container.Size = UDim2.new(1, -20, 0, 150)
Container.Position = UDim2.new(0, 10, 0, 35)
Container.BackgroundTransparency = 1
Container.Parent = MainFrame

-- Nút 1: Khởi tạo Sàn khổng lồ + Ghế + Clone AI
local SpawnBtn = Instance.new("TextButton")
SpawnBtn.Size = UDim2.new(1, 0, 0, 32)
SpawnBtn.Position = UDim2.new(0, 0, 0, 0)
SpawnBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
SpawnBtn.TextColor3 = Color3.fromRGB(0, 255, 128)
SpawnBtn.TextSize = 11
SpawnBtn.Font = Enum.Font.GothamBold
SpawnBtn.Text = "🚀 1. Spawn Giant Floor & Clone"
SpawnBtn.Parent = Container
local UIC_1 = Instance.new("UICorner") UIC_1.CornerRadius = UDim.new(0, 6) UIC_1.Parent = SpawnBtn

-- Nút 2: Swap vị trí qua lại
local SwapBtn = Instance.new("TextButton")
SwapBtn.Size = UDim2.new(1, 0, 0, 32)
SwapBtn.Position = UDim2.new(0, 0, 0, 38)
SwapBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
SwapBtn.TextColor3 = Color3.fromRGB(255, 170, 0)
SwapBtn.TextSize = 11
SwapBtn.Font = Enum.Font.GothamBold
SwapBtn.Text = "🔄 2. Swap Pos: [ REAL: SEATED ]"
SwapBtn.Parent = Container
local UIC_2 = Instance.new("UICorner") UIC_2.CornerRadius = UDim.new(0, 6) UIC_2.Parent = SwapBtn

-- Nút 3: Đổi góc nhìn Camera
local SwitchCamBtn = Instance.new("TextButton")
SwitchCamBtn.Size = UDim2.new(1, 0, 0, 32)
SwitchCamBtn.Position = UDim2.new(0, 0, 0, 76)
SwitchCamBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
SwitchCamBtn.TextColor3 = Color3.fromRGB(100, 200, 255)
SwitchCamBtn.TextSize = 11
SwitchCamBtn.Font = Enum.Font.GothamBold
SwitchCamBtn.Text = "👁️ 3. View: [ CLONE ]"
SwitchCamBtn.Parent = Container
local UIC_3 = Instance.new("UICorner") UIC_3.CornerRadius = UDim.new(0, 6) UIC_3.Parent = SwitchCamBtn

-- Nút 4: Reset toàn bộ
local ResetBtn = Instance.new("TextButton")
ResetBtn.Size = UDim2.new(1, 0, 0, 32)
ResetBtn.Position = UDim2.new(0, 0, 0, 114)
ResetBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
ResetBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
ResetBtn.TextSize = 11
ResetBtn.Font = Enum.Font.GothamBold
ResetBtn.Text = "🔄 Reset All State"
ResetBtn.Parent = Container
local UIC_4 = Instance.new("UICorner") UIC_4.CornerRadius = UDim.new(0, 6) UIC_4.Parent = ResetBtn

local InfoLabel = Instance.new("TextLabel")
InfoLabel.Size = UDim2.new(1, -20, 0, 25)
InfoLabel.Position = UDim2.new(0, 10, 0, 190)
InfoLabel.BackgroundTransparency = 1
InfoLabel.Text = "Status: Ready."
InfoLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
InfoLabel.TextSize = 10
InfoLabel.Font = Enum.Font.Gotham
InfoLabel.Parent = MainFrame

local activeClone = nil
local activeSeat = nil
local giantFloor = nil
local viewingClone = true
local isRealSeated = true

SpawnBtn.MouseButton1Click:Connect(function()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") or not character:FindFirstChildOfClass("Humanoid") then
        InfoLabel.Text = "Status: Character not found!"
        return
    end
    
    local hrp = character.HumanoidRootPart
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    local currentCF = hrp.CFrame
    
    if activeClone then pcall(function() activeClone:Destroy() end) activeClone = nil end
    if activeSeat then pcall(function() activeSeat:Destroy() end) activeSeat = nil end
    if giantFloor then pcall(function() giantFloor:Destroy() end) giantFloor = nil end
    
    -- 1. Tạo Sàn Khổng Lồ ở bên dưới (Kích thước lớn 2048x2048, cách vị trí hiện tại 10 stud xuống dưới)
    giantFloor = Instance.new("Part")
    giantFloor.Name = "UndergroundGiantFloor"
    giantFloor.Size = Vector3.new(2048, 5, 2048)
    giantFloor.CFrame = currentCF - Vector3.new(0, 10, 0)
    giantFloor.Anchored = true
    giantFloor.CanCollide = true
    giantFloor.Transparency = 0.5 -- Bán trong suốt để dễ quan sát hoặc đổi thành 1 nếu muốn tàng hình hoàn toàn
    giantFloor.Color = Color3.fromRGB(35, 35, 45)
    giantFloor.Parent = workspace
    
    -- 2. Tạo Ghế Ô Tô ngay trên sàn khổng lồ đó
    activeSeat = Instance.new("Seat")
    activeSeat.Name = "HiddenArmorSeat"
    activeSeat.Size = Vector3.new(3, 1, 3)
    activeSeat.CFrame = giantFloor.CFrame + Vector3.new(0, 4, 0)
    activeSeat.Anchored = false
    activeSeat.CanCollide = false
    activeSeat.Transparency = 1
    activeSeat.Parent = workspace
    
    -- Cho nhân vật thật ngồi xuống ghế
    humanoid.Sit = true
    task.wait(0.1)
    activeSeat:Sit(humanoid)
    
    -- Tàng hình nhân vật thật
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") then
            part.Transparency = 1
        end
    end
    
    -- 3. Tạo Clone xuất hiện trên mặt đất chính để AI điều khiển
    character.Archivable = true
    activeClone = character:Clone()
    character.Archivable = false
    activeClone.Name = "AI_GiantFloor_Clone"
    activeClone.Parent = workspace
    activeClone:SetPrimaryPartCFrame(currentCF)
    
    local cloneHrp = activeClone:FindFirstChild("HumanoidRootPart")
    local cloneHum = activeClone:FindFirstChildOfClass("Humanoid")
    
    if cloneHum then Camera.CameraSubject = cloneHum end
    viewingClone = true
    isRealSeated = true
    
    SwitchCamBtn.Text = "👁️ 3. View: [ CLONE ]"
    SwapBtn.Text = "🔄 2. Swap Pos: [ REAL: SEATED ]"
    InfoLabel.Text = "Status: Giant floor created. Real body seated safely."
    
    if _G.GiantFloorConnection then _G.GiantFloorConnection:Disconnect() end
    
    -- 4. Vòng lặp AI điều khiển Clone săn mục tiêu
    _G.GiantFloorConnection = RunService.RenderStepped:Connect(function()
        if activeClone and cloneHum and cloneHrp then
            local targetHrp = nil
            local minDist = math.huge
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (plr.Character.HumanoidRootPart.Position - cloneHrp.Position).Magnitude
                    if d < minDist then
                        minDist = d
                        targetHrp = plr.Character.HumanoidRootPart
                    end
                end
            end
            if targetHrp then
                cloneHum:MoveTo(targetHrp.Position)
            end
        end
    end)
end)

-- Nút Swap vị trí qua lại giữa Real Body (trên sàn khổng lồ) và Clone (trên mặt đất)
SwapBtn.MouseButton1Click:Connect(function()
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") or not activeClone or not activeSeat then
        InfoLabel.Text = "Status: Spawn system first!"
        return
    end
    
    local realHrp = character.HumanoidRootPart
    local realHum = character:FindFirstChildOfClass("Humanoid")
    local cloneHrp = activeClone:FindFirstChild("HumanoidRootPart")
    
    if isRealSeated then
        -- TRƯỜNG HỢP 1: Từ Real Body (Đang ngồi trên sàn khổng lồ) -> Đổi chỗ lên mặt đất thay cho Clone
        realHum.Sit = false
        task.wait(0.05)
        realHrp.CFrame = cloneHrp.CFrame
        
        -- Mang ghế và bắt Clone dịch chuyển xuống sàn khổng lồ ngồi vào ghế
        if giantFloor then
            activeSeat.CFrame = giantFloor.CFrame + Vector3.new(0, 4, 0)
        end
        local cloneHum = activeClone:FindFirstChildOfClass("Humanoid")
        if cloneHum then
            cloneHum.Sit = true
            activeSeat:Sit(cloneHum)
        end
        
        isRealSeated = false
        SwapBtn.Text = "🔄 2. Swap Pos: [ REAL: ON SURFACE ]"
        SwapBtn.TextColor3 = Color3.fromRGB(0, 255, 128)
        InfoLabel.Text = "Status: Swapped! Real body is now at Clone position."
    else
        -- TRƯỜNG HỢP 2: Từ vị trí mặt đất -> Trở về ngồi trên sàn khổng lồ bên dưới
        local cloneHum = activeClone:FindFirstChildOfClass("Humanoid")
        if cloneHum then cloneHum.Sit = false end
        task.wait(0.05)
        
        -- Đưa Clone lên vị trí mặt đất
        cloneHrp.CFrame = realHrp.CFrame
        
        -- Kéo Real body trở lại ghế trên sàn khổng lồ bên dưới
        realHrp.CFrame = activeSeat.CFrame + Vector3.new(0, 2, 0)
        realHum.Sit = true
        activeSeat:Sit(realHum)
        
        isRealSeated = true
        SwapBtn.Text = "🔄 2. Swap Pos: [ REAL: SEATED ]"
        SwapBtn.TextColor3 = Color3.fromRGB(255, 170, 0)
        InfoLabel.Text = "Status: Swapped back! Real body is safely seated on giant floor."
    end
end)

SwitchCamBtn.MouseButton1Click:Connect(function()
    local character = LocalPlayer.Character
    if not character or not activeClone then
        InfoLabel.Text = "Status: Spawn system first!"
        return
    end
    
    viewingClone = not viewingClone
    local realHum = character:FindFirstChildOfClass("Humanoid")
    local cloneHum = activeClone:FindFirstChildOfClass("Humanoid")
    
    if viewingClone then
        if cloneHum then Camera.CameraSubject = cloneHum end
        SwitchCamBtn.Text = "👁️ 3. View: [ CLONE ]"
        SwitchCamBtn.TextColor3 = Color3.fromRGB(100, 200, 255)
        InfoLabel.Text = "Status: Viewing Clone"
    else
        if realHum then Camera.CameraSubject = realHum end
        SwitchCamBtn.Text = "👁️ 3. View: [ REAL BODY ]"
        SwitchCamBtn.TextColor3 = Color3.fromRGB(255, 170, 0)
        InfoLabel.Text = "Status: Viewing Real Body"
    end
end)

ResetBtn.MouseButton1Click:Connect(function()
    if _G.GiantFloorConnection then
        _G.GiantFloorConnection:Disconnect()
        _G.GiantFloorConnection = nil
    end
    if activeClone then pcall(function() activeClone:Destroy() end) activeClone = nil end
    if activeSeat then pcall(function() activeSeat:Destroy() end) activeSeat = nil end
    if giantFloor then pcall(function() giantFloor:Destroy() end) giantFloor = nil end
    
    local character = LocalPlayer.Character
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Transparency = 0
            end
        end
        local realHum = character:FindFirstChildOfClass("Humanoid")
        if realHum then 
            realHum.Sit = false
            Camera.CameraSubject = realHum 
        end
    end
    
    viewingClone = false
    isRealSeated = true
    SwapBtn.Text = "🔄 2. Swap Pos: [ REAL: SEATED ]"
    SwitchCamBtn.Text = "👁️ 3. View: [ REAL BODY ]"
    InfoLabel.Text = "Status: Reset completed."
end)
